CREATE TABLE [Conclusion] (
   [id] uniqueidentifier,
   [date] datetime,
   PRIMARY KEY ([id]),
  );

  CREATE TABLE [ResettingTop] (
   [id] uniqueidentifier,
   [date] datetime,
   PRIMARY KEY ([id]),
  );